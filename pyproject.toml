[project]
name = "kiwoomstock"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pandas",
    "numpy",
    "pykrx",
]
[tool.uv]
required-environments = ["cpython-3.12-win_amd64"]
